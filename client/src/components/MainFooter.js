import { Box } from '@mui/material'
import { margin } from '@mui/system'
import React from 'react'

const MainFooter = () => {
  return (
      <Box sx={{position: 'absolute', width: "100%", marginTop: '2rem', backgroundColor:'black', color: "white", display: "flex", justifyContent: "center", marginBottom: "0px"}}>
    <div>Allyson Footer here</div>
     </Box>
  )
}

export default MainFooter